# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
"on": pull_request
jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: make build
        working-directory: ./app
        env:
          NODE_ENV: production
          REACT_APP_ENVIRONMENT: preview
          REACT_APP_AUTH_URL: ${{ vars.AUTH_URL }}

      - uses: FirebaseExtended/action-hosting-deploy@0e16177d01e10c4560245404f959e75a9c633533
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_CORDLESS_A6502 }}"
          projectId: cordless-a6502
          target: cordless-app
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
